pipeline {
    agent any
    stages {
        stage('Call API') {
            steps {
                sh './src/main/zap/visium3.sh'
                //sh './src/main/zap/visium2.sh'
            }
        }
    }
}
